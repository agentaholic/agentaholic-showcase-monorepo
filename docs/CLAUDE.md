@./docs/practices/MARKDOWN_CLI_SNIPPETS.md

## Markdown file names

Although TypeScript filenames are supposed to be in camel case, markdown documents that exist in the `docs/` folder should be in all caps delimited by underscores, such as `SCREENS_VS_PAGES.md` for example.
