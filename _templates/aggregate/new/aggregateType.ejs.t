---
to: src/services/<%= serviceName %>/aggregates/<%= aggregateName %>/types/<%= aggregateName %>Aggregate.ts
---
export type <%= aggregateName %>Aggregate = {
  id: string
  // TODO: Add aggregate properties here
} 